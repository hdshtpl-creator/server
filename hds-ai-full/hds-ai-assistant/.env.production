# Cấu hình dùng khi build production (npm run build đọc file này).
# Deploy một máy chủ: nginx phục vụ giao diện tĩnh và reverse-proxy /api sang
# FastAPI cùng origin, nên chỉ cần đường dẫn tương đối. KHÔNG cần CORS.
VITE_API_BASE_URL=/api
